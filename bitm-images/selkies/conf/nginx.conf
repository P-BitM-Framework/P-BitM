server {
    listen 127.0.0.1:8080;

    location / {
        proxy_pass http://p-bitm-CAMPAIGN_ID:8080/api/sessions/;
        proxy_ssl_verify off;

        proxy_set_header Host CAMPAIGN_IP;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Victim-API-Key "VICTIM_API_KEY";
    }
}
